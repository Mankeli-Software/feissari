# Copy this file to .env and fill values. Do NOT commit the .env file to version control.

# Firebase Project ID (required for Firestore)
# Example: FIREBASE_PROJECT_ID=your-project-id
FIREBASE_PROJECT_ID=your-project-id

# Firebase Service Account Key
# Two supported formats:
# 1) Raw JSON string (single-line). Be sure to wrap in single quotes in your shell or .env if it contains spaces/newlines.
#    Example: FIREBASE_SERVICE_ACCOUNT_KEY='{"type":"service_account",...}'
# 2) Base64-encoded JSON (recommended to avoid newline/quoting issues).
#    Example (macOS): export FIREBASE_SERVICE_ACCOUNT_KEY=$(base64 /path/to/key.json)
FIREBASE_SERVICE_ACCOUNT_KEY=

# Optional: server port
PORT=3001

# NODE_ENV defaults to development; tests set NODE_ENV=test
NODE_ENV=development
